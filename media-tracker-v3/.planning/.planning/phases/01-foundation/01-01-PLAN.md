---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - media-tracker-v3/package.json
  - media-tracker-v3/tsconfig.json
  - media-tracker-v3/next.config.ts
  - media-tracker-v3/src/lib/supabase/client.ts
  - media-tracker-v3/src/lib/supabase/server.ts
  - media-tracker-v3/src/middleware.ts
  - media-tracker-v3/.env.local
  - media-tracker-v3/src/styles/globals.css
  - media-tracker-v3/src/app/layout.tsx
  - media-tracker-v3/src/app/page.tsx
autonomous: true
requirements:
  - UI-01

must_haves:
  truths:
    - "Next.js 14 App Router project exists with TypeScript and Tailwind configured"
    - "Supabase client libraries are installed and both server/browser client factories exist"
    - "Middleware refreshes Supabase session on every request"
    - "npm run dev starts the dev server without errors"
    - "shadcn/ui is initialized with New York style and CSS variables"
  artifacts:
    - path: "media-tracker-v3/package.json"
      provides: "Project manifest with all Phase 1 dependencies"
      contains: "next"
    - path: "media-tracker-v3/src/lib/supabase/server.ts"
      provides: "Server-side Supabase client factory"
      exports: ["createClient"]
    - path: "media-tracker-v3/src/lib/supabase/client.ts"
      provides: "Browser-side Supabase client factory"
      exports: ["createClient"]
    - path: "media-tracker-v3/src/middleware.ts"
      provides: "Session refresh middleware"
      contains: "middleware"
  key_links:
    - from: "media-tracker-v3/src/lib/supabase/server.ts"
      to: "@supabase/ssr"
      via: "createServerClient import"
      pattern: "createServerClient"
    - from: "media-tracker-v3/src/middleware.ts"
      to: "@supabase/ssr"
      via: "createServerClient import"
      pattern: "createServerClient"

user_setup:
  - service: supabase
    why: "Database and auth backend for the entire app"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> anon/public (may show as 'publishable key' on newer projects)"
    dashboard_config:
      - task: "Create a new Supabase project"
        location: "https://supabase.com/dashboard -> New Project"
---

<objective>
Scaffold the Next.js 14 App Router project with TypeScript, Tailwind CSS, Supabase client libraries, shadcn/ui, and all Phase 1 dependencies installed. This creates the empty project shell that every subsequent plan and phase builds inside.

Purpose: Establish the project structure, dependency tree, and Supabase connectivity before any design system or deployment work begins.
Output: A running Next.js 14 dev server with Supabase client factories, middleware, and shadcn/ui initialized.
</objective>

<execution_context>
@/Users/dlgvnbyr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dlgvnbyr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 14 project and install all Phase 1 dependencies</name>
  <files>
    media-tracker-v3/package.json
    media-tracker-v3/tsconfig.json
    media-tracker-v3/next.config.ts
    media-tracker-v3/src/app/layout.tsx
    media-tracker-v3/src/app/page.tsx
    media-tracker-v3/src/styles/globals.css
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Installation section, Standard Stack table, Pattern 4 for Tailwind tokens)
    .planning/phases/01-foundation/01-UI-SPEC.md (Design System section, Color section)
  </read_first>
  <action>
1. Run `npx create-next-app@14 media-tracker-v3 --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"` from the project root (`/Users/dlgvnbyr/Documents/Hicheel/Deep Learning/media-tracker/local`).

2. cd into `media-tracker-v3` and install runtime dependencies:
   `npm install @supabase/supabase-js @supabase/ssr lucide-react framer-motion`

3. Initialize shadcn/ui:
   `npx shadcn@latest init`
   Select: New York style, slate base color, CSS variables: yes.
   If the CLI prompts interactively and cannot be automated, use flags or answer defaults.

4. Add Phase 1 shadcn components:
   `npx shadcn@latest add button tabs tooltip separator`

5. After scaffold, check which Tailwind version was installed (v3 vs v4). If v3, design tokens go in `tailwind.config.ts`. If v4, tokens go in `globals.css` via `@theme` directive.

6. Configure Tailwind design tokens — add these color extensions (in whichever config format the installed version uses):
   - `base.DEFAULT`: `#0a0a0f`
   - `base.surface`: `#10101a`
   - `base.elevated`: `#16162a`
   - `accent.DEFAULT`: `#4f7cff`
   - `accent.muted`: `#2a3f80`
   - `accent.silver`: `#a8b4cc`
   - `backdrop.top`: `transparent`
   - `backdrop.mid`: `rgba(10, 10, 15, 0.6)`
   - `backdrop.base`: `#0a0a0f`

7. Update `src/app/layout.tsx`:
   - Import Inter font from `next/font/google`
   - Set `<html className="dark">` with Inter font class
   - Set `<body className="bg-base font-sans text-white antialiased">`

8. Update `src/app/page.tsx` to redirect to `/movies`:
   ```typescript
   import { redirect } from 'next/navigation'
   export default function Home() { redirect('/movies') }
   ```

9. Verify `npm run dev` starts without errors. Verify `npm run build` completes.

IMPORTANT: Use `create-next-app@14` explicitly — NOT `@latest` which would install Next.js 16.x.
  </action>
  <verify>
    <automated>cd media-tracker-v3 && grep '"next"' package.json | grep -q '14' && grep -q '@supabase/ssr' package.json && grep -q 'framer-motion' package.json && grep -q 'lucide-react' package.json && ls src/components/ui/button.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `media-tracker-v3/package.json` contains `"next": "^14` (NOT v15 or v16)
    - `media-tracker-v3/package.json` contains `"@supabase/supabase-js"`, `"@supabase/ssr"`, `"lucide-react"`, `"framer-motion"`
    - `media-tracker-v3/src/components/ui/button.tsx` exists (shadcn button installed)
    - `media-tracker-v3/src/components/ui/tabs.tsx` exists (shadcn tabs installed)
    - `media-tracker-v3/src/components/ui/tooltip.tsx` exists (shadcn tooltip installed)
    - Tailwind config contains `#0a0a0f` (base.DEFAULT color token)
    - Tailwind config contains `#4f7cff` (accent.DEFAULT color token)
    - `src/app/layout.tsx` contains `className="dark"` on html element
    - `src/app/page.tsx` contains `redirect('/movies')`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Next.js 14 project scaffolded with all dependencies, shadcn/ui initialized with 4 components, Tailwind design tokens configured, root page redirects to /movies, build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create Supabase client factories and session middleware</name>
  <files>
    media-tracker-v3/src/lib/supabase/server.ts
    media-tracker-v3/src/lib/supabase/client.ts
    media-tracker-v3/src/middleware.ts
    media-tracker-v3/.env.local
    media-tracker-v3/.gitignore
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 1: Supabase Client Initialization, Pattern 2: Supabase Middleware, Environment Variables section)
    media-tracker-v3/package.json (confirm @supabase/ssr is installed)
  </read_first>
  <action>
1. Create `src/lib/supabase/server.ts` — server-side client factory using `createServerClient` from `@supabase/ssr`:
   - Import `createServerClient` from `@supabase/ssr`
   - Import `cookies` from `next/headers`
   - Export async function `createClient()` that creates a server client with cookie handling
   - Use `process.env.NEXT_PUBLIC_SUPABASE_URL!` and `process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!`
   - Implement `getAll()` and `setAll()` cookie methods per RESEARCH.md Pattern 1

2. Create `src/lib/supabase/client.ts` — browser-side client factory using `createBrowserClient` from `@supabase/ssr`:
   - Import `createBrowserClient` from `@supabase/ssr`
   - Export function `createClient()` that creates a browser client
   - Use same env var names: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`

3. Create `src/middleware.ts` — session refresh middleware per RESEARCH.md Pattern 2:
   - Import `createServerClient` from `@supabase/ssr`
   - Import `NextResponse, NextRequest` from `next/server`
   - Export async `middleware(request: NextRequest)` function
   - Create supabase client with cookie relay between request and response
   - Call `await supabase.auth.getUser()` to trigger session refresh
   - Return the supabaseResponse
   - Export config matcher: `['/((?!_next/static|_next/image|favicon.ico).*)']`

4. Create `.env.local` with placeholder values:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
   NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key-here
   ```

5. Verify `.gitignore` includes `.env.local` (create-next-app should have this already; confirm and add if missing).

6. Create a temporary test route `src/app/movies/page.tsx` that verifies the Supabase connection:
   ```typescript
   import { createClient } from '@/lib/supabase/server'
   export default async function MoviesPage() {
     const supabase = await createClient()
     const { error } = await supabase.from('_test').select('*').limit(1)
     return (
       <div className="p-8 text-white">
         <h1 className="text-2xl font-semibold">Movies</h1>
         <p>Supabase: {error?.code === '42P01' ? 'Connected (table not found — expected)' : error ? 'Error: ' + error.message : 'Connected'}</p>
       </div>
     )
   }
   ```
  </action>
  <verify>
    <automated>cd media-tracker-v3 && grep -q "createServerClient" src/lib/supabase/server.ts && grep -q "createBrowserClient" src/lib/supabase/client.ts && grep -q "middleware" src/middleware.ts && grep -q "NEXT_PUBLIC_SUPABASE_URL" .env.local && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/supabase/server.ts` exports `createClient` function using `createServerClient` from `@supabase/ssr`
    - `src/lib/supabase/server.ts` references `process.env.NEXT_PUBLIC_SUPABASE_URL`
    - `src/lib/supabase/server.ts` references `process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
    - `src/lib/supabase/client.ts` exports `createClient` function using `createBrowserClient` from `@supabase/ssr`
    - `src/middleware.ts` exports `middleware` function and calls `supabase.auth.getUser()`
    - `src/middleware.ts` exports `config` with matcher pattern
    - `.env.local` contains `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`
    - `.env.local` is listed in `.gitignore`
    - `src/app/movies/page.tsx` exists and imports from `@/lib/supabase/server`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Supabase server and browser client factories created, session refresh middleware in place, env vars templated, movies route verifies connection, build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- `package.json` shows Next.js 14.x (not 15 or 16)
- All 4 shadcn/ui components installed (button, tabs, tooltip, separator)
- Supabase client factories export `createClient` from both server.ts and client.ts
- Middleware file exists with session refresh logic
- `.env.local` not committed to git
</verification>

<success_criteria>
- Next.js 14 App Router project exists at `media-tracker-v3/` with TypeScript and Tailwind
- All Phase 1 dependencies installed: @supabase/supabase-js, @supabase/ssr, lucide-react, framer-motion
- shadcn/ui initialized with button, tabs, tooltip, separator components
- Tailwind design tokens configured with midnight blue/silver palette
- Supabase client factories (server + browser) and middleware created
- `npm run dev` starts, `npm run build` succeeds
- Root `/` redirects to `/movies`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
