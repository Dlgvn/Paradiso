---
phase: 02-authentication
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - media-tracker-v3/src/components/layout/Sidebar.tsx
  - media-tracker-v3/src/components/layout/BottomNav.tsx
autonomous: false
requirements: [AUTH-03]

user_setup:
  - service: supabase
    why: "Authentication requires real Supabase project credentials"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> anon/public"
      - name: NEXT_PUBLIC_SITE_URL
        source: "Set to https://media-tracker-v3.vercel.app for production, http://localhost:3000 for local dev"
    dashboard_config:
      - task: "Verify email confirmation setting"
        location: "Supabase Dashboard -> Authentication -> Providers -> Email -> Confirm email toggle"
      - task: "Update Vercel env vars with real Supabase credentials (currently placeholder)"
        location: "Vercel Dashboard -> Project Settings -> Environment Variables"

must_haves:
  truths:
    - "User can log out from any page via a logout button in sidebar and bottom nav"
    - "After logout user is redirected to /auth"
    - "Full auth flow works end-to-end: signup -> land on /movies -> logout -> login -> /movies"
  artifacts:
    - path: "media-tracker-v3/src/components/layout/Sidebar.tsx"
      provides: "Logout button in sidebar"
      contains: "signOut"
    - path: "media-tracker-v3/src/components/layout/BottomNav.tsx"
      provides: "Logout button in bottom nav"
      contains: "signOut"
  key_links:
    - from: "media-tracker-v3/src/components/layout/Sidebar.tsx"
      to: "signOut Server Action"
      via: "form action or onClick"
      pattern: "signOut"
    - from: "media-tracker-v3/src/components/layout/BottomNav.tsx"
      to: "signOut Server Action"
      via: "form action or onClick"
      pattern: "signOut"
---

<objective>
Add logout button to Sidebar and BottomNav, then verify the complete auth flow end-to-end.

Purpose: AUTH-03 requires logout from any page. The sidebar (desktop) and bottom nav (mobile) are the persistent navigation — adding a logout button here fulfills the requirement. The checkpoint verifies the full auth flow works with real Supabase credentials.

Output: Updated Sidebar and BottomNav with logout functionality. User-verified working auth flow.
</objective>

<execution_context>
@/Users/dlgvnbyr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dlgvnbyr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-01-SUMMARY.md
@.planning/phases/02-authentication/02-02-SUMMARY.md

<interfaces>
From media-tracker-v3/src/app/(auth)/auth/actions.ts:
```typescript
export async function signOut()  // calls supabase.auth.signOut() then redirect('/auth')
```

From media-tracker-v3/src/components/layout/Sidebar.tsx:
```typescript
// 'use client' component with icon-only nav items, Tooltip labels
// Currently has 6 nav items (movies, books, series, search, analytics, recommendations)
// Logout button should be added at the bottom of the sidebar, separated from nav items
```

From media-tracker-v3/src/components/layout/BottomNav.tsx:
```typescript
// 'use client' component with 5-item fixed bottom tab bar
// Logout needs to be accessible — either as a nav item or via a user menu
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logout button to Sidebar and BottomNav</name>
  <files>
    media-tracker-v3/src/components/layout/Sidebar.tsx,
    media-tracker-v3/src/components/layout/BottomNav.tsx
  </files>
  <read_first>
    media-tracker-v3/src/components/layout/Sidebar.tsx,
    media-tracker-v3/src/components/layout/BottomNav.tsx,
    media-tracker-v3/src/app/(auth)/auth/actions.ts
  </read_first>
  <action>
    **Sidebar.tsx changes:**
    1. Import `signOut` from `@/app/(auth)/auth/actions`
    2. Import `LogOut` icon from `lucide-react`
    3. Add a logout button at the BOTTOM of the sidebar, visually separated from the navigation items. Use `mt-auto` on a wrapper div to push it to the bottom of the flex column.
    4. The logout button is a `<form action={signOut}><button type="submit">` wrapping a `LogOut` icon. Style it the same as nav items but with a distinct hover color: `hover:bg-red-500/20 hover:text-red-400`.
    5. Wrap in `Tooltip` with `side="right"` and content "Log out" — consistent with existing nav item tooltip pattern.
    6. Ensure the sidebar is `flex flex-col h-full` (or equivalent) so `mt-auto` pushes the logout to the bottom.

    **BottomNav.tsx changes:**
    1. Import `signOut` from `@/app/(auth)/auth/actions`
    2. Import `LogOut` icon from `lucide-react`
    3. Replace the last nav item (likely recommendations or the least-used one — read the current items to decide) OR add a 6th item at the end for logout. Since 5 items + 1 logout = 6 items on mobile bottom nav can be tight, the better approach is to replace the least critical item. However, if all 5 are important, add logout as a smaller icon at the far right.
    4. Alternative approach if space is tight: Instead of a full nav item, add a small logout icon button at the right edge of the bottom nav bar, styled more subtly (smaller, muted color, `text-accent-silver hover:text-red-400`).
    5. Logout button is a `<form action={signOut}><button type="submit">` with `LogOut` icon.
    6. The form must NOT have the standard nav item active-state logic since logout is an action, not a route.
  </action>
  <verify>
    <automated>cd media-tracker-v3 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - media-tracker-v3/src/components/layout/Sidebar.tsx contains `import { signOut } from`
    - Sidebar.tsx contains `LogOut` (lucide-react icon import)
    - Sidebar.tsx contains `action={signOut}` or `onClick` calling signOut
    - Sidebar.tsx contains `Log out` or `Logout` or `log out` (tooltip text)
    - media-tracker-v3/src/components/layout/BottomNav.tsx contains `import { signOut } from`
    - BottomNav.tsx contains `LogOut`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Logout button added to both Sidebar (bottom-pinned with tooltip) and BottomNav. Both call signOut Server Action via form submission. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete auth flow end-to-end</name>
  <files>none</files>
  <action>
    Human verification of the complete Phase 2 auth system. No code changes — this is a verification-only checkpoint.
    The executor should start the dev server (`cd media-tracker-v3 && npm run dev`) and present the verification steps to the user.
  </action>
  <what-built>
    Complete authentication system:
    - Route group structure (auth pages standalone, app pages with AppShell)
    - Sign up, login, logout, forgot password, update password flows
    - Middleware route protection (redirect to /auth when unauthenticated)
    - Frosted glass auth UI with animated view transitions
    - Logout button in sidebar and bottom nav
  </what-built>
  <how-to-verify>
    **Prerequisites:** Ensure Supabase env vars are real (not placeholder) in .env.local and Vercel dashboard. Ensure NEXT_PUBLIC_SITE_URL is set.

    1. Start dev server: `cd media-tracker-v3 && npm run dev`
    2. Open http://localhost:3000 in a fresh browser (or incognito)
    3. **Verify redirect:** Should automatically redirect to /auth (middleware protection)
    4. **Verify auth page:** Should see "Media Tracker" logo, tagline, frosted glass card with login form, no sidebar or bottom nav
    5. **Verify signup:** Click "Sign up" toggle, fill email + password + confirm password, submit. Should redirect to /movies
    6. **Verify AppShell:** On /movies, sidebar (desktop) and bottom nav (mobile) should be visible
    7. **Verify logout:** Click logout button in sidebar (or bottom nav on mobile). Should redirect to /auth
    8. **Verify login:** Enter credentials, submit. Should redirect to /movies
    9. **Verify session persistence:** Close tab, reopen http://localhost:3000. Should still be on /movies (not redirected to /auth)
    10. **Verify redirect param:** Log out, try to visit http://localhost:3000/books. Should redirect to /auth?redirect=/books. After login, should land on /books (not /movies)
    11. **Verify forgot password:** Click "Forgot password?", enter email, submit. Should see "Check your inbox" message
    12. **Verify error display:** Try logging in with wrong password. Should see error banner above form

    Test on both desktop and 375px mobile viewport.
  </how-to-verify>
  <verify>Human approves the auth flow or describes issues</verify>
  <done>User has verified: signup, login, logout, session persistence, redirect preservation, forgot password, error display. Phase 2 authentication is complete.</done>
  <resume-signal>Type "approved" to mark Phase 2 complete, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `cd media-tracker-v3 && npm run build` exits 0
2. Sidebar has logout button with LogOut icon
3. BottomNav has logout functionality
4. Human verifies full auth flow: signup -> /movies -> logout -> /auth -> login -> /movies
5. Human verifies redirect persistence, session persistence, error display
</verification>

<success_criteria>
- Logout button visible and functional in both sidebar and bottom nav
- Complete auth flow works: signup, login, logout, forgot password
- Middleware redirects work correctly (unauthenticated -> /auth, authenticated -> /movies)
- Session persists across tab close/reopen
- Redirect param preserves original destination after login
- Auth errors display in banner
- User approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-03-SUMMARY.md`
</output>
