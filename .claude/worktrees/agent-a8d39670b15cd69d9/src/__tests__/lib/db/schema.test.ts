describe('MediaTrackerDB (PWA-02)', () => {
  it.todo('opens with version 1 and tables [library_items, sync_queue]')
  it.todo('library_items.bulkPut + .toArray() round-trips MediaItem records')
  it.todo('library_items can be queried by user_id index')
  it.todo('sync_queue auto-increments id on add')
})
