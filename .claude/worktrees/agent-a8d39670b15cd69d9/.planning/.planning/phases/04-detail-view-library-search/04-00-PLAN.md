---
phase: 04-detail-view-library-search
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - jest.config.ts
  - jest.setup.ts
  - src/__tests__/components/detail/CinematicBackdrop.test.tsx
  - src/__tests__/components/detail/ItemDetailPage.test.tsx
  - src/__tests__/lib/search/filter.test.ts
  - src/__tests__/components/search/LibrarySearchInput.test.tsx
autonomous: true
requirements:
  - DETAIL-01
  - DETAIL-02
  - SRCH-01
  - SRCH-02

must_haves:
  truths:
    - "npm test runs without error and all 4 stub files pass"
    - "Jest is configured for Next.js with TypeScript and React Testing Library"
  artifacts:
    - path: "jest.config.ts"
      provides: "Jest configuration for Next.js App Router project"
      contains: "nextJest"
    - path: "jest.setup.ts"
      provides: "RTL jest-dom matchers setup"
      contains: "@testing-library/jest-dom"
    - path: "src/__tests__/components/detail/CinematicBackdrop.test.tsx"
      provides: "Test stub for DETAIL-02 cinematic backdrop"
    - path: "src/__tests__/components/detail/ItemDetailPage.test.tsx"
      provides: "Test stub for DETAIL-01 item detail sheet"
    - path: "src/__tests__/lib/search/filter.test.ts"
      provides: "Test stub for SRCH-01 filter logic"
    - path: "src/__tests__/components/search/LibrarySearchInput.test.tsx"
      provides: "Test stub for SRCH-02 debounced search input"
  key_links: []
---

<objective>
Install Jest + React Testing Library and create 4 test stub files required by VALIDATION.md Wave 0.

Purpose: Establishes the Nyquist sampling contract so every subsequent task can run automated tests alongside tsc verification. Without this, all task verifications rely solely on tsc --noEmit, leaving behavioral correctness unsampled.

Output: Jest config, setup file, and 4 test stubs that pass with --passWithNoTests.
</objective>

<execution_context>
@/Users/dlgvnbyr/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dlgvnbyr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-detail-view-library-search/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Jest + RTL, create config, and write 4 test stubs</name>
  <files>
    jest.config.ts,
    jest.setup.ts,
    src/__tests__/components/detail/CinematicBackdrop.test.tsx,
    src/__tests__/components/detail/ItemDetailPage.test.tsx,
    src/__tests__/lib/search/filter.test.ts,
    src/__tests__/components/search/LibrarySearchInput.test.tsx
  </files>
  <read_first>
    package.json,
    tsconfig.json,
    .planning/phases/04-detail-view-library-search/04-VALIDATION.md
  </read_first>
  <action>
**Step 1: Install dependencies**
```bash
npm install --save-dev jest @jest/types ts-jest @testing-library/react @testing-library/jest-dom @testing-library/user-event jest-environment-jsdom @types/jest
```

**Step 2: Add test script to package.json**
Add `"test": "jest"` to the `scripts` section of package.json.

**Step 3: Create jest.config.ts**
```typescript
import type { Config } from '@jest/types'
import nextJest from 'next/jest'

const createJestConfig = nextJest({ dir: './' })

const config: Config.InitialOptions = {
  testEnvironment: 'jsdom',
  setupFilesAfterSetup: ['<rootDir>/jest.setup.ts'],
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1',
  },
  testPathIgnorePatterns: ['<rootDir>/node_modules/', '<rootDir>/.next/'],
}

export default createJestConfig(config)
```

Note: Use `setupFilesAfterSetup` key name. If next/jest requires `setupFilesAfterSetup` to be spelled `setupFilesAfterSetup`, read the next/jest docs from the installed package. The standard Jest key is `setupFilesAfterSetup`. next/jest may map it — check and use whichever works: try `setupFilesAfterSetup` first, fall back to `setupFiles` if needed. The key point is that `jest.setup.ts` gets loaded before tests.

**Step 4: Create jest.setup.ts**
```typescript
import '@testing-library/jest-dom'
```

**Step 5: Create 4 test stub files**

Each stub imports `describe` and `it` from Jest globals and contains a single placeholder test that passes immediately. These stubs will be filled with real assertions as Plans 01 and 02 execute.

**src/__tests__/components/detail/CinematicBackdrop.test.tsx:**
```typescript
describe('CinematicSheetBackdrop', () => {
  it('should render without crashing', () => {
    // Stub — real assertions added during Plan 01 execution
    expect(true).toBe(true)
  })
})
```

**src/__tests__/components/detail/ItemDetailPage.test.tsx:**
```typescript
describe('ItemDetailSheet', () => {
  it('should render without crashing', () => {
    // Stub — real assertions added during Plan 01 execution
    expect(true).toBe(true)
  })
})
```

**src/__tests__/lib/search/filter.test.ts:**
```typescript
describe('filterLibrary', () => {
  it('should return all items when query is empty', () => {
    // Stub — real assertions added during Plan 02 execution
    expect(true).toBe(true)
  })
})
```

**src/__tests__/components/search/LibrarySearchInput.test.tsx:**
```typescript
describe('LibrarySearchInput', () => {
  it('should render without crashing', () => {
    // Stub — real assertions added during Plan 02 execution
    expect(true).toBe(true)
  })
})
```

Create the directory structure: `src/__tests__/components/detail/`, `src/__tests__/components/search/`, `src/__tests__/lib/search/` using `mkdir -p` before writing files.
  </action>
  <verify>
    <automated>cd /Users/dlgvnbyr/Documents/Hicheel/Deep\ Learning/media-tracker/local/media-tracker-v3 && npm test -- --testPathPattern=detail\|filter\|LibrarySearch --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>Jest is installed and configured for Next.js. All 4 test stubs pass. `npm test -- --testPathPattern=detail|filter|LibrarySearch --passWithNoTests` exits 0 with 4 test suites passing. The "test" script exists in package.json.</done>
</task>

</tasks>

<verification>
1. `npm test -- --testPathPattern=detail|filter|LibrarySearch --passWithNoTests` passes with 4 suites
2. `npm test` passes (full suite, same 4 stubs)
3. jest.config.ts exists with nextJest configuration
4. jest.setup.ts imports @testing-library/jest-dom
</verification>

<success_criteria>
- Jest + RTL installed as devDependencies
- jest.config.ts uses next/jest createJestConfig
- 4 test stub files exist at paths specified in VALIDATION.md
- All stubs pass when run
- package.json has "test": "jest" script
</success_criteria>

<output>
After completion, create `.planning/phases/04-detail-view-library-search/04-00-SUMMARY.md`
</output>
